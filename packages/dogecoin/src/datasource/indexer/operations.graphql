query DogecoinBalance($address: String!) {
  dogecoin {
    balances(address: $address) {
      address
      amount {
        value
      }
      asset {
        chain
        contract
        decimals
        id
        image
        name
        price {
          amount
        }
        symbol
      }
    }
  }
}

query GetDogecoinFees {
  dogecoin {
    fee {
      high
      low
      medium
    }
  }
}

query GetDogecoinStatus {
  dogecoin {
    status {
      lastBlock
    }
  }
}

query GetDogecoinTransactions($address: String!, $pageNumber: Int!, $pageSize: Int!, $dateRange: OptDateRange!, $blockRange: OptBlockRange!) {
  dogecoin {
    transactions(address: $address, pageNumber: $pageNumber, pageSize: $pageSize, dateRange: $dateRange, blockRange: $blockRange) {
      hash
      blockNumber
      blockIndex
      status
      timestamp
      inputs {
        address
        amount {
          value
        }
      }
      outputs {
        amount {
          value
        }
        address
      }
      fee {
        value
      }
      balanceChange {
        value
      }
    }
  }
}